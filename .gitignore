__pycache__/
*.pyc
*.egg-info/
.DS_Store
*.swp
.env
runs/
assets/
# ...except the BTR-80A, which the pursuit env and the detector datasets need.
# CC BY 4.0, redistributed with assets/vehicles/btr80/ATTRIBUTION.md.
!assets/
assets/*
!assets/vehicles/
assets/vehicles/*
!assets/vehicles/btr80/
.venv/
.vesper_jobs.json
.vesper_session.json
.vesper_session.log

# PBR scan sets are fetched by scripts/fetch_pbr.py (CC0 ambientCG), not committed
textures/pbr/
.ms_dataset_links.csv

# GPU-box working space: datasets, checkpoints, venvs. Never snapshotted.
scratch/

# web server runtime state / logs
.vesper_envbuilds.json
.vesper_api.log
.envbuild_*.log

# detector serving state (which RF-DETR is up on the box)
.vesper_detector.json
